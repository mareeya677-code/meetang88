# 📱 วิธีใช้งาน PWA Installer สำหรับ Meetang88

## 🚀 วิธีติดตั้งบนมือถือ:

### ขั้นตอนที่ 1: อัปโหลดไฟล์
อัปโหลดไฟล์ทั้ง 3 ไฟล์นี้ขึ้น Web Server:
- `install.html` (หน้าเว็บหลัก)
- `manifest.json` (ข้อมูล PWA)
- `sw.js` (Service Worker)

ตัวอย่าง URL: `https://yourserver.com/meetang88/install.html`

### ขั้นตอนที่ 2: ส่งลิงก์ให้ลูกค้า
ให้ลูกค้าเปิดลิงก์ `install.html` บนมือถือ

### ขั้นตอนที่ 3: กดปุ่มติดตั้ง
ลูกค้ากดปุ่ม **"📥 ติดตั้งแอป"** บนหน้าเว็บ

### ขั้นตอนที่ 4: ยืนยันการติดตั้ง
ระบบจะแสดง popup ให้กด **"เพิ่ม"** หรือ **"ติดตั้ง"**

✅ เสร็จแล้ว! แอปจะปรากฏบนหน้าจอหลัก

---

## 🖥️ หรือทดสอบบนเครื่อง (localhost):

1. เปิด PowerShell
2. ไปที่โฟลเดอร์ Meetang88:
   ```powershell
   cd "C:\Users\ADMIN\Desktop\Meetang88"
   ```

3. รัน Python Web Server:
   ```powershell
   python -m http.server 8000
   ```

4. เปิดมือถือ → เชื่อมต่อ WiFi เดียวกับคอม
5. เปิดเบราว์เซอร์บนมือถือ → ไปที่:
   ```
   http://192.168.x.x:8000/install.html
   ```
   (แทน 192.168.x.x ด้วย IP ของคอมพิวเตอร์)

---

## 📝 คุณสมบัติของหน้าติดตั้ง:

✓ ปุ่มติดตั้งอัตโนมัติ
✓ แสดงวิธีทำด้วยตนเอง (ถ้าติดตั้งอัตโนมัติไม่ได้)
✓ รองรับทั้ง Android และ iPhone
✓ ออกแบบสวยงาม ใช้งานง่าย
✓ เปลี่ยนเส้นทางไปเว็บหลังติดตั้งสำเร็จ

---

## 🎯 หมายเหตุ:

- ไฟล์ต้องอยู่บน **HTTPS** หรือ **localhost** เท่านั้น
- ถ้าทดสอบบน localhost ต้องใช้เครื่องเดียวกัน หรือเชื่อมต่อ WiFi เดียวกัน
- บน iPhone จะไม่มีปุ่มติดตั้งอัตโนมัติ ต้องทำตามวิธีด้วยตนเอง

---

## 💡 วิธีหา IP ของคอมพิวเตอร์:

```powershell
ipconfig
```

หา **IPv4 Address** เช่น `192.168.1.100`